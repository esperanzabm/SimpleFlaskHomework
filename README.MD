# 🧠 MiFlaskApp - SimpleFlaskHomework

Aplicación web desarrollada en **Flask (Python)** con conexión a **MongoDB**, que incluye autenticación de roles (admin, manager, usuario) y plantillas HTML.

## 🚀 Funcionalidades principales
- Login y registro de usuarios.
- Roles: `admin`, `manager`, `user`.
- Visualización de usuarios (solo para roles autorizados).
- Página pública con lista de usuarios visibles.
- Conexión a MongoDB configurada mediante variables de entorno.

---

## 🛠️ Requisitos
- Python 3.8 o superior  
- MongoDB (local o en la nube con MongoDB Atlas)
- Git

---

## ⚙️ Instalación y ejecución

### 1️⃣ Clonar el repositorio
```bash
git clone https://github.com/esperanzabm/MiFlaskApp.git
cd MiFlaskApp/SimpleFlaskHomework